'use client'

import Link from 'next/link'
import { useEffect, useMemo, useState } from 'react'
import { createClient } from '@/lib/supabase/client'

type Product = { id: string; name: string; description: string | null; category: string | null; price: number | null; currency: string; image_url: string | null; external_url: string | null; affiliate_url: string | null }

export default function ProdutosSuplementosPage() {
  const [products, setProducts] = useState<Product[]>([])
  const [category, setCategory] = useState('Todos')
  const [query, setQuery] = useState('')
  const [selected, setSelected] = useState<Product | null>(null)
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    const load = async () => {
      const supabase = createClient()
      const { data } = await supabase.from('store_products').select('id,name,description,category,price,currency,image_url,external_url,affiliate_url').eq('active', true).order('created_at', { ascending: false })
      setProducts((data || []) as Product[])
      setLoading(false)
    }
    load()
  }, [])

  const categories = useMemo(() => ['Todos', ...Array.from(new Set(products.map(p => p.category).filter(Boolean) as string[]))], [products])
  const visible = useMemo(() => products.filter(p => (category === 'Todos' || p.category === category) && `${p.name} ${p.description || ''}`.toLowerCase().includes(query.toLowerCase())), [products, category, query])
  const money = (p: Product) => p.price == null ? 'Consultar' : new Intl.NumberFormat('pt-BR', { style: 'currency', currency: p.currency || 'BRL' }).format(Number(p.price))

  const buy = async (p: Product) => {
    const supabase = createClient()
    await supabase.from('store_clicks').insert({ product_id: p.id })
    const url = p.affiliate_url || p.external_url
    if (url) window.open(url, '_blank', 'noopener,noreferrer')
  }

  return <main className="shop"><header><Link href="/dashboard" className="brand"><span>P</span> PRETREINO</Link><nav><Link href="/dashboard">Dashboard</Link><Link className="active" href="/produtos-suplementos">Produtos & Suplementos</Link><Link href="/loja">Planos</Link></nav></header><section className="hero"><div><span>PRETREINO STORE</span><h1>Produtos & <em>Suplementos.</em></h1><p>Uma seleção pensada para acompanhar a sua jornada. Explore os produtos publicados pela equipa PRETREINO e escolha o que faz sentido para o seu momento.</p></div><div className="hero-badge"><b>✦</b><strong>Curadoria PRETREINO</strong><small>Produtos publicados pelo seu painel administrativo.</small></div></section><section className="toolbar"><div className="categories">{categories.map(c => <button key={c} className={category === c ? 'selected' : ''} onClick={() => setCategory(c)}>{c}</button>)}</div><label className="search">⌕<input value={query} onChange={e => setQuery(e.target.value)} placeholder="Pesquisar produto..." /></label></section>{loading ? <section className="state">A preparar a loja...</section> : visible.length === 0 ? <section className="state"><strong>{products.length === 0 ? 'A loja está a preparar os primeiros produtos.' : 'Nenhum produto encontrado.'}</strong><span>{products.length === 0 ? 'Assim que um produto for publicado no painel administrativo, ele aparecerá aqui automaticamente.' : 'Experimente outra pesquisa ou categoria.'}</span></section> : <section className="grid">{visible.map(p => <article className="card" key={p.id} onClick={() => setSelected(p)}><div className="visual">{p.image_url ? <img src={p.image_url} alt={p.name} /> : <div className="placeholder">P</div>}<span>{p.category || 'DESTAQUE'}</span></div><div className="body"><small>PRETREINO SELECT</small><h2>{p.name}</h2><p>{p.description || 'Produto selecionado para acompanhar a sua jornada PRETREINO.'}</p><div className="bottom"><strong>{money(p)}</strong><button onClick={e => { e.stopPropagation(); buy(p) }}>Ver produto <b>→</b></button></div></div></article>)}</section>}

{selected && <div className="modal-backdrop" onClick={() => setSelected(null)}><div className="modal" onClick={e => e.stopPropagation()}><button className="close" onClick={() => setSelected(null)}>×</button><div className="modal-image">{selected.image_url ? <img src={selected.image_url} alt={selected.name} /> : <div className="placeholder">P</div>}</div><div className="modal-copy"><small>{selected.category || 'DESTAQUE'}</small><h2>{selected.name}</h2><p>{selected.description || 'Produto selecionado para acompanhar a sua jornada PRETREINO.'}</p><strong>{money(selected)}</strong><button className="buy" onClick={() => buy(selected)}>Ir para o produto →</button></div></div></div>}

<style jsx>{`*{box-sizing:border-box}.shop{min-height:100vh;background:radial-gradient(circle at 75% 0%,rgba(124,58,237,.16),transparent 34%),#070810;color:#f8f8fb;font-family:Arial,Helvetica,sans-serif;padding-bottom:90px}.shop header{height:78px;padding:0 clamp(22px,6vw,86px);border-bottom:1px solid rgba(255,255,255,.07);display:flex;align-items:center;justify-content:space-between;position:sticky;top:0;background:rgba(7,8,16,.82);backdrop-filter:blur(18px);z-index:8}.brand{display:flex;align-items:center;gap:10px;color:#fff;font-weight:900;letter-spacing:.08em}.brand span{width:35px;height:35px;display:grid;place-items:center;border-radius:10px;background:linear-gradient(145deg,#a855f7,#4f46e5);box-shadow:0 0 25px rgba(139,92,246,.3)}nav{display:flex;gap:25px}nav a{color:#777b8e;font-size:11px;font-weight:800}nav a.active,nav a:hover{color:#d8b4fe}.hero{max-width:1160px;margin:auto;padding:78px 24px 45px;display:flex;justify-content:space-between;align-items:end;gap:40px}.hero>div:first-child>span{font-size:10px;color:#a78bfa;font-weight:900;letter-spacing:.18em}.hero h1{font-size:clamp(45px,7vw,76px);line-height:.95;letter-spacing:-.06em;margin:14px 0}.hero em{font-style:normal;background:linear-gradient(90deg,#c084fc,#60a5fa);-webkit-background-clip:text;color:transparent}.hero p{max-width:690px;color:#9296a8;line-height:1.75;font-size:14px}.hero-badge{min-width:260px;padding:20px;border:1px solid rgba(192,132,252,.2);border-radius:18px;background:linear-gradient(145deg,rgba(124,58,237,.16),rgba(255,255,255,.025));display:grid;grid-template-columns:38px 1fr;gap:3px 10px}.hero-badge b{grid-row:span 2;width:38px;height:38px;border-radius:11px;display:grid;place-items:center;background:rgba(139,92,246,.18);color:#d8b4fe}.hero-badge strong{font-size:12px}.hero-badge small{color:#777b8e;line-height:1.45}.toolbar{max-width:1160px;margin:auto;padding:0 24px 28px;display:flex;justify-content:space-between;gap:20px;align-items:center;border-bottom:1px solid rgba(255,255,255,.07)}.categories{display:flex;gap:7px;flex-wrap:wrap}.categories button{border:1px solid rgba(255,255,255,.08);background:#0c0e17;color:#85899a;border-radius:999px;padding:9px 14px;font-size:10px;font-weight:800;cursor:pointer}.categories button.selected,.categories button:hover{background:rgba(139,92,246,.16);color:#e9ddff;border-color:rgba(192,132,252,.25)}.search{height:38px;width:230px;border:1px solid rgba(255,255,255,.08);background:#0c0e17;border-radius:10px;display:flex;align-items:center;gap:8px;padding:0 11px;color:#777b8e}.search input{border:0;outline:0;background:transparent;color:#fff;width:100%;font:inherit;font-size:11px}.grid{max-width:1160px;margin:32px auto 0;padding:0 24px;display:grid;grid-template-columns:repeat(3,1fr);gap:16px}.card{border:1px solid rgba(255,255,255,.08);background:linear-gradient(180deg,#0d0f18,#0a0b12);border-radius:22px;overflow:hidden;cursor:pointer;transition:transform .2s,border-color .2s,box-shadow .2s}.card:hover{transform:translateY(-5px);border-color:rgba(192,132,252,.3);box-shadow:0 18px 45px rgba(0,0,0,.35)}.visual{height:245px;position:relative;background:radial-gradient(circle at 50% 25%,rgba(168,85,247,.24),transparent 48%),#111321}.visual img{width:100%;height:100%;object-fit:cover}.visual>span{position:absolute;left:14px;top:14px;background:rgba(7,8,16,.72);backdrop-filter:blur(8px);border:1px solid rgba(255,255,255,.1);border-radius:999px;padding:6px 9px;font-size:8px;color:#e9ddff;font-weight:900;letter-spacing:.12em}.placeholder{height:100%;display:grid;place-items:center;font-size:62px;font-weight:900;color:#c084fc}.body{padding:20px}.body small,.modal-copy small{font-size:8px;color:#a78bfa;letter-spacing:.16em;font-weight:900}.body h2{font-size:20px;margin:8px 0}.body p{min-height:54px;color:#85899a;font-size:12px;line-height:1.55}.bottom{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-top:20px}.bottom strong{font-size:20px}.bottom button,.buy{border:1px solid rgba(192,132,252,.22);background:linear-gradient(90deg,rgba(124,58,237,.3),rgba(79,70,229,.2));color:#f1e8ff;border-radius:10px;padding:11px 13px;font-size:10px;font-weight:900;cursor:pointer}.bottom button b{margin-left:5px}.state{max-width:1160px;margin:32px auto;padding:55px 25px;border:1px dashed rgba(255,255,255,.12);border-radius:20px;text-align:center;color:#777b8e;display:flex;flex-direction:column;gap:9px}.state strong{color:#fff;font-size:16px}.modal-backdrop{position:fixed;inset:0;background:rgba(0,0,0,.72);backdrop-filter:blur(10px);display:grid;place-items:center;padding:22px;z-index:20}.modal{position:relative;width:min(850px,100%);display:grid;grid-template-columns:1fr 1fr;background:#0c0e17;border:1px solid rgba(255,255,255,.1);border-radius:24px;overflow:hidden;box-shadow:0 30px 100px rgba(0,0,0,.6)}.modal-image{min-height:430px;background:#111321}.modal-image img{width:100%;height:100%;object-fit:cover}.modal-copy{padding:48px 38px;display:flex;flex-direction:column;justify-content:center}.modal-copy h2{font-size:34px;letter-spacing:-.04em;margin:10px 0}.modal-copy p{color:#85899a;line-height:1.7;font-size:13px}.modal-copy>strong{font-size:28px;margin:15px 0 24px}.buy{padding:14px}.close{position:absolute;right:14px;top:14px;width:35px;height:35px;border-radius:50%;border:1px solid rgba(255,255,255,.12);background:rgba(7,8,16,.7);color:#fff;font-size:22px;z-index:2;cursor:pointer}@media(max-width:850px){nav{display:none}.hero{padding-top:50px;display:block}.hero-badge{margin-top:25px}.toolbar{display:block}.search{width:100%;margin-top:12px}.grid{grid-template-columns:1fr}.modal{grid-template-columns:1fr}.modal-image{height:280px;min-height:0}.modal-copy{padding:28px}.modal-copy h2{font-size:28px}}`}</style></main>
