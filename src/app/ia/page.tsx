'use client'

import Link from 'next/link'
import { useEffect, useState } from 'react'
import { createClient } from '@/lib/supabase/client'

type Conversation = { id: string; title: string | null; updated_at: string }

export default function IAPage() {
  const [items, setItems] = useState<Conversation[]>([])
  const [prompt, setPrompt] = useState('')
  const [loading, setLoading] = useState(true)
  const [saving, setSaving] = useState(false)
  const [message, setMessage] = useState('')

  useEffect(() => { (async () => { const s=createClient(); const {data:u}=await s.auth.getUser(); if(!u.user){location.href='/login';return}; const {data}=await s.from('ai_conversations').select('id,title,updated_at').eq('user_id',u.user.id).order('updated_at',{ascending:false}).limit(12); setItems((data||[]) as Conversation[]); setLoading(false) })() }, [])
  async function start() { if(!prompt.trim()) return; setSaving(true); const s=createClient(); const {data:u}=await s.auth.getUser(); if(!u.user)return; const {data,error}=await s.from('ai_conversations').insert({user_id:u.user.id,title:prompt.trim().slice(0,70),context:{source:'dashboard'}}).select('id,title,updated_at').single(); if(error){setMessage('Não foi possível iniciar a conversa.');setSaving(false);return} setItems([data as Conversation,...items]); setPrompt(''); setMessage('Conversa criada. A camada de IA pode continuar a partir deste contexto.'); setSaving(false) }
  return <main className="premium"><header><Link href="/dashboard" className="brand"><span>P</span> PRETREINO</Link><Link href="/dashboard">← Dashboard</Link></header><div className="wrap"><section className="hero"><small>PRETREINO IA</small><h1>Seu espaço de<br/><em>inteligência.</em></h1><p>Centralize conversas e contexto da sua jornada para que a experiência possa evoluir consigo.</p></section><section className="composer"><small>NOVA CONVERSA</small><textarea value={prompt} onChange={e=>setPrompt(e.target.value)} placeholder="Ex.: quero organizar minha próxima semana de treino..."/><button onClick={start} disabled={saving}>{saving?'A criar...':'Começar conversa →'}</button>{message&&<p>{message}</p>}</section><section><div className="head"><div><small>HISTÓRICO</small><h2>Suas conversas</h2></div></div>{loading?<div className="empty">A carregar...</div>:items.length===0?<div className="empty">Ainda não existem conversas guardadas.</div>:<div className="list">{items.map(x=><article key={x.id}><span>✦</span><div><strong>{x.title||'Conversa PRETREINO IA'}</strong><small>{new Date(x.updated_at).toLocaleDateString('pt-BR')}</small></div><b>→</b></article>)}</div>}</section></div><style jsx>{`.premium{min-height:100vh;background:radial-gradient(circle at 75% 0%,rgba(124,58,237,.18),transparent 35%),#070810;color:#fff;font-family:Arial;padding-bottom:80px}.premium header{height:78px;border-bottom:1px solid rgba(255,255,255,.08);display:flex;justify-content:space-between;align-items:center;padding:0 6vw}.brand{display:flex;gap:10px;align-items:center;color:#fff;font-weight:900;letter-spacing:.08em}.brand span{width:35px;height:35px;border-radius:10px;display:grid;place-items:center;background:linear-gradient(145deg,#a855f7,#4f46e5)}header>a:last-child{color:#9296a8;font-size:12px}.wrap{max-width:1000px;margin:auto;padding:65px 24px}.hero small,.head small,.composer>small{color:#a78bfa;font-size:10px;font-weight:900;letter-spacing:.18em}.hero h1{font-size:clamp(45px,7vw,72px);line-height:.95;letter-spacing:-.06em;margin:12px 0}.hero em{font-style:normal;background:linear-gradient(90deg,#c084fc,#60a5fa);-webkit-background-clip:text;color:transparent}.hero p{max-width:650px;color:#9296a8;line-height:1.7}.composer{margin:40px 0 55px;padding:22px;border:1px solid rgba(255,255,255,.08);background:#0c0e17;border-radius:20px}.composer textarea{display:block;width:100%;min-height:120px;margin:15px 0;border:1px solid rgba(255,255,255,.08);border-radius:12px;background:#080a11;color:#fff;padding:14px;resize:vertical}.composer button{border:0;border-radius:10px;padding:13px 18px;background:linear-gradient(90deg,#7c2ff0,#4f46e5);color:#fff;font-weight:900}.composer p,.empty{color:#85899a;font-size:12px}.head h2{margin:8px 0 20px}.list{display:grid;gap:9px}.list article{display:flex;align-items:center;gap:14px;padding:18px;border:1px solid rgba(255,255,255,.07);border-radius:14px;background:#0c0e17}.list article>span{color:#c084fc}.list article div{display:grid;gap:5px;flex:1}.list article small{color:#6f7385;font-size:10px}.list article>b{color:#a78bfa}.empty{padding:35px;border:1px dashed rgba(255,255,255,.1);border-radius:15px;text-align:center}`}</style></main>
}
