# PRETREINO — etapa 3

1. `npm install`
2. Configure `NEXT_PUBLIC_SUPABASE_URL` e `NEXT_PUBLIC_SUPABASE_ANON_KEY` conforme `env.example`.
3. Aplique as migrations em `supabase/migrations/` no projeto Supabase.
4. `npm run dev`

Etapa 3: Perfil Fitness persistido em `fitness_profiles`; evolução em `fitness_measurements`; RLS por utilizador; Dashboard com acesso ao módulo Evolução.
